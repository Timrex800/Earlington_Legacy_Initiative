# Local environment example (DO NOT COMMIT real secrets)
# Copy this file to `.env` or set env vars in your hosting provider
# For security, never commit real API keys to source control.

# Cloudinary: set your Cloudinary URL in this format:
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# Example (replace placeholders with your real credentials):
# CLOUDINARY_URL=cloudinary://abcd1234:efgh5678@drj03twbh

# Vite env variables (client-safe) should use VITE_ prefix if required
# VITE_REASONER_ENDPOINT=/.netlify/functions/reasoner

