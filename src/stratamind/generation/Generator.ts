// Generator removed. Provide safe fallback for any imports.
export function generateFixSuggestion(_issue: string): string {
  return 'Reasoner removed: no automated suggestions available.';
}

export default generateFixSuggestion;
